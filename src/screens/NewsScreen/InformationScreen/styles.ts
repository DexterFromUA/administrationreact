import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  full: {
    flex: 1,
    paddingVertical: 20,
    marginHorizontal: 10,
  },
});

export default styles;
