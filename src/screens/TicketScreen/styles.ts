import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  active: {
    flex: 1,
    marginTop: 5,
  },
  expired: {
    flex: 2,
  },
});

export default styles;
